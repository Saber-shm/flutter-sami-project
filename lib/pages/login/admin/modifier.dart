import 'package:flutter/material.dart';

class AdminModifier extends StatelessWidget {
  const AdminModifier({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
