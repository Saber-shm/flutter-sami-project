import 'package:flutter/material.dart';

class COM_ajouter_ordre extends StatelessWidget {
  const COM_ajouter_ordre({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
